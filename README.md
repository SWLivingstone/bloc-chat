A simple chat app with the ability to create and use chatrooms built using AngularJS

## Run the Application

Instal dependencies

```
$ npm install
```

Run the application server:

```
$ npm start
```

The server will start up. To stop the server, press `cntrl + c`.
